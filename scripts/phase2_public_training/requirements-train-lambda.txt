# YOLO fine-tune only (Lambda / Linux + NVIDIA). Install PyTorch+CUDA first — see setup_lambda_training.sh
# YOLO26 requires a recent Ultralytics release; upgrade on the instance if yolo26l.pt fails to load.
ultralytics>=8.3.0
Pillow>=10.0.0
huggingface_hub>=0.20.0
PyYAML>=6.0
protobuf>=4.25.0,<5
